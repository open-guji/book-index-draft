# 已知缺陷登記

**此處記的都是「查清了、但決定暫不修」的東西。** 每條須寫明：範圍（有多少）、
成因（為何如此）、為何暫不修、以及**修的時候會踩到什麼**。

只寫「有問題」而不寫成因與範圍的，不要放進來——那種東西下次還得從頭查一遍，
記了等於沒記。

---

## ⚠ 2026-09-03 全目錄覆核：本目錄整體滯後於庫之實況

本目錄各檔多成於 2026-08-2x，其後庫經**分片改制、文本移倉、千頃堂書目全量升格
（12,598 條）**三場大動，`promotions.json` 已自 239 條漲到 **135,472 條**。
以現庫逐條實測，所記之數多已不實。**下手之前先看此節，勿據舊數行事。**

**一、已消解者（毋須再辦）**

| 記 | 舊記 | 2026-09-03 實測 |
|---|---|---|
| 整理本繫連落空 | 307 條（另 production 側 217 處） | **0**（book-text 102,681 處引用零懸空） |
| 目錄分片退化 | 「待擇期停機施行」 | **已施行**，全庫已取尾三字元 |
| `period未決` | 5,499 條 | **餘 211**，且皆域外漢籍（日 167／朝 10／英 3），非缺陷 |
| `殘名撰人` | 169 條 | **餘 9**，皆漢志「臣某」之體，原文止一字，非缺陷 |
| `C1-題名重出待併` | 59 組 | **0 組兩條俱存**，確已併訖 |
| `S2`／`S6` 待退役 entity | 11 個 | 8 個已退役；3 個（樊紹述、栁宗元、居士）works 已清空，僅餘空殼待刪 |
| 玉函山房等未繫 | 68 條 | 全庫整理本未繫僅 **33** 條 |

**二、記錄仍屬實、原樣待辦者**

- `2026-08-26-Book清零輪三件待決.json`（古今韻會舉要／周易本義附錄纂疏／四書標題）
  ——三案條目**一條未動**，並存如故。
- `2026-08-26-聚珍版卷數待覈五種.json`、`2026-08-26-再造善本繫Work存疑六則.json`
  ——卷數與繫連**原樣未改**。
- `S4-隋唐升格後跨桶同題待裁`——118 組中 **107 組仍兩條俱存**（見下「情勢已變」）。

**三、情勢已變、判斷前提不再成立者**

- **`S4` 之「不便併」已不成立**：該檔立時之由是「一在 production 一在 draft，
  跨倉併之險大」，今實測 **134 對俱已在 production**，跨倉之礙消失，可逕辦。
- **整理本已不在兩倉，移入 `book-text`**（見專節）。`chk.py` 已跟上（其
  `_assets()` 兼掃 `_TEXT_ROOT`），**毋須再改**；然**自己另寫的一次性腳本**
  凡只掃 draft／production 者，掃整理本必空，而空掃不報錯——此為新的踩坑處。

**四、`chk.py` 現行基線（2026-09-03 實跑，補上 Windows 路徑修正後）**

```
解析失敗 0    檔案未入索引 0    自我關聯 0    空關聯 0    懸空關聯 0
Book→Work 單向 0    Work→Book 單向 0
related_works[].title 漂移 0    related_works (id,relation) 重複 0
派生欄位與重算不符 1（單一 Book 之 _has_image）
索引指向不存在檔案 1
```

**除二處各 1 條外，諸驗全綠。** 庫之實況遠好於本目錄各檔所給之印象——
彼多成於各輪整理**進行之中**，記的是當時之待辦，其後多已隨批次消解而未回填。

**五、本輪新查出、前所未記者**

- **`chk.py` 於 Windows 誤報全庫 140,210 檔分片錯置**：判準本身無誤（已改尾三字元），
  錯在拿 `p.split('/')` 切 `glob` 於 Windows 所回之反斜杠路徑，切不開遂得空表而全不相等。
  一行可修（`p.replace(os.sep,'/')`），詳見〈目錄分片退化〉節。Linux／WSL 上此驗是綠的。
- **`promotions.json` 有 40 條指向不存在之 production 檔**：逐條核之
  **皆係有意併／刪之殘留**（7 個 entity 正是 2026-09-03 各輪質檢所刪之「武帝」
  「何承夫」「劉道薈」「顏延之逸」等；33 個 Work 係《韓非子|韓子》
  《舊唐書|唐書》之類異名併條），**併刪之時未同步清 promotions 目**。
  非資料之壞，然 `validate-promotions` 諸驗據此表而行，宜清。
- **已退役之壞 entity 復被新批次繫上**：`S2` 明記「濮王泰即唐太宗第四子李泰，
  一人二稱」而判其當退役，然 2026-08-25「題名自著撰人」批次又據
  「庫中同名 entity 唯一」將新條《濮王泰集》(`d59f2og1r7cy`) 繫了上去。
  **此正是本目錄開篇所戒之「記了不修，下次整理時被引向錯誤結論」，且已真實發生。**
  凡據「同名 entity 唯一」自動繫連者，**須先查該 entity 是否在本目錄之待退役名單內**。

## 為何要有這個目錄

本庫的缺陷有一大類**不觸發任何校驗**：欄位有值、非空、無懸空關聯，`chk.py` 全綠，
但語義是壞的。它們的代價不在當下，而在**下一次整理時把你引向錯誤的結論**——
最常見的形態是「庫中查不到 → 新建 → 重出」。

所以這裡的每一條，實質上都是一張**動手前的對照表**。

## 開工前該對哪一張

| 你要做的事 | 先看 |
|---|---|
| 據外部目錄新建 work | `殘名撰人.md`（比對不合未必是真沒有）、`疑似重出.md`、`簡轉繁過度轉換.md`、`隋志注中亡書.md`（比對之四法） |
| 處置外集／附錄／年譜之類部帙 | `SCHEMA.md`〈版本附屬部帙：不新建 Work〉（判準是「曾否單行」「出於誰手」，不是看書名） |
| 合併 work | **先看 `SCHEMA.md`〈同題二條，何時是重出、何時是二書〉**（十條判準，皆自 B3′ 七百六十九次合併實測所得：同志卷異則疑／異志卷異不疑、志書「又」例、漢志分列二略、出土簡帛之題名巧合、撰人異名看份量、比對前須簡繁歸一、合併後須同步之五處）；再看 `疑似重出.md`、`全庫同名異書核對.md`（各批之逐類記錄） |
| 合併／對齊佚文 | `後漢書輯本二源未對齊.md`（**2026-08-22 覆勘更正二事**：二源已各自標明輯家可逕分兩堆；「以 heading 為錨」不可直接用——姚本用傳主名、周本用范書篇名，交集為 0，須拆周本標目取人名。候選見 `D2-姚汪二本對齊候選.json`） |
| 選某一時代之集合 | `period未決.md`（period 軸之判準與其界。**其「宋 4,562 未決」已不實**——2026-09-03 實測全庫僅餘 211 條未定代，且九成是域外漢籍） |
| **繫 entity（尤其自動繫連）** | **`2026-09-03-待退役entity被自動繫連復用.json`**（「同名唯一」只證無第二個同字面者，**不證此 entity 立得住**；壞 entity 恰恰都是唯一的，且因已解繫而 works 少，「works 少」反是危險之號）、`2026-09-03-歷代通名帝王entity混淆待覈.json`（武帝／明帝／文帝之屬歷代重複）、`S2`／`S6` 之待退役名單 |
| 補存佚（loss_status） | `秦漢loss_status.md`（單字與「此書」自指詞兩路皆已驗證不可用，勿重試；輯佚條目結構標記一路可用已施行） |
| 繫連撰人 | `殘名撰人.md`（甲類可機械回復，乙類須讀案語）、`簡轉繁過度轉換.md`（幹寶／硃育非其名） |
| 解析匯入文本 | `簡轉繁過度轉換.md`（同一志書之內即不一致，勿假設某源必正）、`漢志覆核.md`（四庫本卷末有考證；kanripo 之 &KRxxxx;） |

## 現有條目

| 檔 | 範圍 | 成因 | 狀態 |
|---|---|---|---|
| `殘名撰人.md` | 169 條 → **餘 9** | 二因：匯入固定截二字（14）／志書原文只寫姓（155） | **2026-09-03 覆核：136 條已補全**（「賀」→ 全名、「韓」→「韓坰」、「筠」→「吳筠」等），24 條隨併條消解，**餘 9 條非缺陷**——皆漢志「臣某」之體（臣彭、臣說賦、臣義賦、秦零陵令信、待詔臣饒心術…），原文本止一字，無可補 |
| `疑似重出.md` | 61 組 124 條 | 匯入切分不當；同書兩次著錄 | **已全數處理完畢**（2026-08-10）：38 組已由其後各批次順帶消解（本檔曾未同步）；餘 23 組覆核：9 組確認非重出（漢志同名異書 5＋出土文獻異批次簡帛 4）、13 組真重出已合併、1 組（大觀堂文集）確認為全本／節本之別，以 related_works 記關係，不合併 |
| `簡轉繁過度轉換.md` | 3,691 處 1,571 檔 | 匯入之文本由簡體回轉，一簡多繁未擇 | **已修**，chk 基線 0；餘字未普查 |
| `D3-玉函山房未繫節複測.json` | 68 節 → 餘 48 | 「本庫同題 N 條，不可決」；B3′ 併池消解同題後複測，以題名收窄繫 19、以撰人再收窄繫 1 | **繫 20**。餘 48 中三十餘條輯本亦不著撰人而庫中同題各不著撰人，無可比之欄，須查輯本正文 |
| `隋志注中亡書.md` | 待裁 257（現餘 56）、主條自亡 34 | 隋志之亡書寄於他條之注，匯入時未拆 | 已建 250＋201＝451；餘 56 種判為與既有記錄同書而未建（逐條理由見 `隋志注亡書待裁.json` 之 `skip_reason`）；過程中另發現複合列舉句撰人誤植之新錯型（已修 11 條）與既有記錄題名尾綴數字殘留之瑕疵（3 條，標出未動） |
| `period未決.md` | 5,499 條 → **餘 211** | 宋 4,562（劉宋／趙宋不可別）為大宗 | **2026-09-03 覆核：5,008 條已定代**（91%），280 條隨併條消解。**餘 211 條非中土之書**——日本 167、朝鮮 10、英國 3 等，域外漢籍本不適用中國朝代軸，當別立軸或逕留 null，不必再列為待決 |
| `漢志覆核.md` | 含簡體 141 | 匯入時未捕獲六略三十八種；簡繁混雜 | 六略三十八種 664／664 條已全補；志末大凡驗算已做；整理本反寫缺口 119 條已補；簡體字未改 |
| `經義考未匯入之部分.md` | 6,523 條 | 通貫漢至清，新建規模遠大於前議 | 已補 1,428 條之一源，餘待請 |
| `後漢書輯本二源未對齊.md` | 8 家 | 姚之駰本與汪文臺本並存 | **2026-08-22 五批對齊 750 條**，片段 2,847→2,097。餘姚獨 318、周未配 175——其中司馬彪 135 條係姚本獨收（原疑「八志」之說已證否）。法見該檔〈六〉：人名之錨用盡後改全文比對，門檻 0.85 不可再降 |
| `秦漢loss_status.md` | 1,086 條 | 《後漢藝文志》等係散文考證，非 terse 目錄體 | 已補 48＋42 條（結構化引文＋輯佚條目標記，後者全庫共補 466，餘 26 條待核見同名 json），單字／自指詞兩路試而不可用 |
| `後漢藝文志未繫種.md` | 5 條、另 5 組疑重出 | 整理本 44 類疑漏收（孟子章句類、謚法、政論、荀爽周易注一家）；另查出 5 組疑同書兩 work_id | 737／742 section 已補；5 組重出候選中 4 組已核准合併，1 組維持不併 |
| `三國藝文志未繫種.md` | 96 條 | 整理本（OCR、未粗校）本身覆蓋不全，非比對之失（已比照後漢志之法覆核，非比對方法之誤） | 414／510 已補，餘 96 條存疑未動 |
| `秦漢同名異書核對.md` | 現況以最新普查為準（同名 2+ 條，touch 秦漢一區，已從 271 組降至約 150 組） | 第三步逐條抽驗發現之新礦：撰人被拆成兩個 entity（異體字／本名字互代），及別集「連書省撰人」之另一種撞名形態；另衍生出全庫規模之 Entity 分裂合併（batch13/15/16/18）與連書省撰人 Pattern 3 全庫掃描（batch16） | batch1～18（含 Entity 分裂合併、連書省撰人全庫掃描等）＋batch19（10 組）＋batch20（8 組）＋batch21（3 組）＋batch22（1 組，size=4~6 抽核）＋batch23（2026-08-10，「待核」8 組中 5 組已裁）＋batch24（2026-08-10，size=2/3 現餘 95 組逐組核對完畢，另裁 4 組＋修 1 組）持續進行中；size=2/3 已全數逐組核對完畢，size≥4（約 51 組，含周易注 34、論語注 23 等大宗異撰人集合）尚待逐條覆核；批次腳本教訓已記：entity_id 分組判準之陷阱、`work_ids` 欄位偶有字串型壞值、collated_edition 檔改動須用精準字串替換以免縮排格式化製造巨型 diff、entity 誤配名實不符（如「子貢」entity 實為明人「李勝芳」）；22 組維持待核（22 組中 3 組於 batch23 裁畢，另新增 3 組於 batch24） |

| `C1-撰人跨代誤繫.json` | 166 條（改繫 12／脫繫 96／存疑 2） | 同名異代誤繫——明清之人被繫成隋唐以前之書的撰人（entity 標明 68、標清 62）。偵測法：撰人 Entity 之 period 起年晚於該書 period_upper 終年 | 已處置。存疑 2 條是**上限誤繫而非人物誤繫**（《涉史隨筆》南宋葛洪撰而被《補晉書藝文志》繫上），交 A 車道 |
| `C1-題名自著撰人定代.json` | 686 條 | 撰人只見於題名而 `authors` 欄闕，三判準俱夠不著 | 已定代並補撰人（2026-08-22）。取名之防八條記於 `scripts/c1_period_from_title_author.py` 檔頭 |
| `C1-entity期與上限相斥.json` | 111 條 | C1 依 Entity 推得之 period 與上限相斥者 | 已由 `C1-撰人跨代誤繫` 一輪處置 |
| `C1-併後同題而他條無撰人.json` | 7 組 | 併池後短題與他條同名，而他條無撰人——無從斷其異同，不套「書名＋撰人」式 | 待併池車道查（鐘磬志、崇安記、古今年代歷、建元歷、闕史、修文海、讀書志） |
| `C1-題名重出待併.json` | 59 組 | 補 `authors` 之後 chk「題名重出」才看得見的長短題並存 | **已全數合併**（2026-08-22），chk 該項 0 |
| `B3-撰人名近似待覈.json` | 105 組 | 撰人名一字之差或相含，而兩名在庫中份量相當，判不出是形訛還是真異人 | 待覈。**其中 22 組疑為 Entity 重出**（陳灃／陳澧、陳淳／陳湻、汪克寛／汪克寬）——當併 Entity 而非併 Work |
| `2026-09-03-歷代通名帝王entity混淆待覈.json` | 已修 3 例（14 Work＋3 Entity）；全庫同類未查 | 早期批次僅憑「庫中同名唯一」繫連，未核朝代——「武帝」「明帝」「文帝」歷代重複 | **本目錄現時最活躍之一檔**，內含 2026-09-03 聶崇岐本十輪質檢之全部所得（同名異代、entity 層已訂而 Work 層未同步、「不著撰人」機械誤判 13 條等）。**其教訓可通全庫**：「記錄聲稱已修復不代表資料已修復」「核『已修復』須反查同一 entity 下的姊妹條目」 |
| `2026-09-03-待退役entity被自動繫連復用.json` | 已查實 1 例；同型自動繫連全庫 5,096 處未查 | 本目錄判為「待退役／不當立」之 entity，被別的批次據「同名唯一」重新繫上 | 2026-09-03 查明，未修。**與上一檔是同一病根之二面**：彼記「同名異代之真人被合為一」，此記「已判不當立者被復用」 |
| `2026-09-03-promotions指向已刪production檔40條.json` | 40 條 | 併條／刪條之後未同步清 `promotions.json` 之對照目 | 2026-09-03 查明，未修。**非資料之壞**（墓碑與存留條俱在），然 `validate-promotions` 諸驗據此表而行。清理**不可逕刪**，當改指存留者以存溯源之鏈 |

## 未另立檔而長期擱置者

以下數目穩定，性質已明，一併記於此，不另立檔。
**括號內為 2026-09-03 覆核之現值**，前記之數已舊。

- ~~**整理本繫連落空 307 條（相異 id 82）**~~ → **0，已清**。2026-09-03 實測
  book-text 三仓口徑下 **102,681 處 work_id 引用、零懸空**。前記之數係整理本
  尚在 draft／production 兩倉時之口徑，文本移出後（見〈整理本已移入 book-text〉）
  該病灶隨之消解。
- **整理本 section 級磁鐵 696 組 1,547 題**——異題共指一 work（「晉書一百一十卷」
  「晉書三十六卷」等四題同指一個《晉書》）。與 `疑似重出.md` 是一體兩面。
  （前記 879 題，係兩倉口徑；今於 book-text 全量重算得 696 組／1,547 題。）
- **整理本未繫 33 條**——`unlinked_reason` 就地可讀，不必回查。
  （前記「玉函山房輯佚書未繫 68 條」，今全庫僅餘 33。）

## 待整體處理（2026-08-21 普查，當日皆已清畢）

見同目錄 `全庫待整體處理.md`。撮其要：

| 項 | 量 | 可否機械處置 |
|---|---|---|
| ~~直齋解題文誤截為書名~~ | ~~64~~ → **0** | ✅ 2026-08-21 清畢：53 條追回出處後刪、12 條併或正名 |
| ~~題即部帙通名之裸題 Work~~ | ~~27~~ → **1** | ✅ 2026-08-21 清畢：正名 7、併 2、撤 1、留 1（崇文總目《制集》，志書原題如此）、刪 17 |
| 缺字與亂碼題名 | 普查畢：真缺陷 10 條 | ✅ 2026-08-21 還原 10 條；**餘 3 條組字之字未得**（石肅庵槧、山曆崌山人集、文王篆清娛），須覈四庫總目原本 |
| 無撰人 Work | 27,480／74,634 | **非缺陷**，志書體例本不著撰人 |

## 已結案（檔已刪，結論記於此）

刪掉的是「做完了、其結論已落入資料本身或 SCHEMA」的檔。要查施行明細，
到 git log 找對應 commit；要查某條記錄怎麼來的，看它自己的 `merged_from`
與 `ai_note`——那才是機器可讀的憑據，比這裡的快照可靠。

| 已刪之檔 | 結論 |
|---|---|
| `撰人字首式重出候選.md` 及其 7 個快照 json | 1,072 組全數處理完畢（2026-08-10）。2026-08-21 覆核：**兩側俱存者 0**，確已無殘餘。累計合併 1,116 組，17 組排除。 |
| `題名重出_round1/2已合併.json` | chk「題名重出」現 2，基線 70——即《李氏春秋》／《春秋》與《毛詩大義》／《毛詩大義梁武帝撰》二組，係刻意不併。 |
| `秦漢同名異書核對_batch{1,2,14,15,16,19,20,21,22}已合併.json` | 施行快照，明細見 git log 與各條 `merged_from`。母檔 `秦漢同名異書核對.md` 仍在，其 size≥4 尚未完。 |
| `疑似重出_20260810已合併.json` | 母檔 `疑似重出.md` 仍在。2026-08-21 覆核：61 組中仍有二條以上並存者 8 組，**皆刻意不併**（漢志跨類同名異書＋出土文獻異批次簡帛），且俱在先秦域。 |
| `整理本Work側回鏈`／`整理本落空work_id(s)`／`索引同步`／`人物作品單向`／`出土文獻BookWork互指` 各 `_round1已修復.json` | 一次性修復之快照，對應校驗現皆在基線。 |
| `待建Work清單.md` | 磁鐵拆解專項之待建清單，六組並附錄甲乙俱已施行（2026-08-20）。**其後為 SCHEMA〈版本附屬部帙〉所推翻大半**——所建之外集／附錄類 Work 復撤（見 2026-08-21 三次 commit）。判準已入 SCHEMA，清單本身無再存之理。 |

**一條教訓，值得留下**（原記於 `撰人字首式重出候選.md`）：

> 「一側全無撰人」型的重出候選，驗證時不能只看候選撰人是否「像」人名，
> **必須反查另一側既有記錄之 `authors[]` 是否本身即誤植**。該輪 17 組排除
> 中，僅 3 組屬候選集合外部之撞名，其餘 14 組之「偽撰人」其實已經寫入本庫
> 既有記錄（部分甚至已誤配 CBDB entity）——「州縣提綱」誤析出撰人「州縣」、
> 「梁有演說文」誤析出「梁有」、「太康三年地記」誤析出「太康」。
> 若不逐條反查出處原文，此類錯誤將長期留存而不被 `chk.py` 任何校驗捕捉
> （撰人格式合法、entity 存在、僅語義有誤）。

## 不是缺陷、別去「修」的

| 現象 | 說明 |
|---|---|
| `chk.py` 於 **Windows** 報〈目錄分片錯置 103〉與〈索引欄位不符 110,803〉 | **皆路徑分隔之假陽性，資料無恙。** 二者同因：`glob` 於 Windows 回反斜杠路徑（`Work\a\t\0\…`），而索引存的、比對用的都是正斜杠。修分片一驗須 `p.replace(os.sep,'/')`（見〈目錄分片退化〉節）；〈索引欄位不符〉同理。**Linux／WSL 上二驗俱綠。** 又：分片一驗修後所餘之 103 條，經逐條比對**全係目錄名大小寫之異**（`Work/a/T/` vs id 之 `at0`），而 **git 中記的是小寫 `a/t/`**——僅本機檢出之表象（Windows 檔案系統不辨大小寫），倉庫與線上皆無此事。 |
| 帛書《黃帝四經》四篇（經法 `1ewozu39wyb5w`、十六經 `1ewozu3akzkzq`、稱 `1ewozu3bb7img`、道原 `1ewozu3c1fg96`）無 `subtype: chapter`，與母條 `1ev7xkhjkqygw` 之關係作 `related` 而非 `part_of`／`has_part` | **使用者有意如此**（2026-08-23 確認）。曾被當成「併池所致之退化」報出，不是。索引側之 `subtype` 隨之消失是 reindex 照記錄檔重算之果，正確。 |

## ~~升格遺留：draft 側之資產目錄~~ → **已清畢**（2026-09-03 覆核）

**2026-09-03 實測：draft 側資產目錄 0 個**（`fragments`／`collated_edition` 俱無，
`Work/*/*/*/*/` 一層全空）。清理見 commit `dd0a23ce315`「draft 資產目錄清畢：
51 個目錄、173.5 MB」——逐檔 md5 比對確認 production 有全同副本者方刪。
其後整理本又整體移入 `book-text`（見專節），故此問題**兩重皆已消解**。

下文之「未決」（promote 該不該刪 draft 原件）亦隨之落定：**draft 不留資產**。
本節留作沿革。

---

（以下為原記，存查）

`promote` 把 draft 之資產目錄（`{id}/fragments`、`{id}/collated_edition`）
**整體物理拷貝**到 production，而**不刪 draft 原件**；接著 `rewrite_references`
又把留下來那份的 `work_id` 改指 production id——於是「路徑在 draft、
內文自稱 production」，`chk` 之「輯佚檔 work_id 與路徑不符」即現。

2026-08-23 實測，已升格 239 條中留有 draft 資產目錄者 **5 條**（production 皆已有同一份）：

| draft id | production | draft 側檔數 | 處置 |
|---|---|---:|---|
| `1ev7w0ewrc5c0` 蒼頡篇 | `d59f1il8fnk0` | 1（fragments） | **已刪**（與 production 逐字節相同） |
| `1eujf2fs4v280` 欽定四庫全書總目 | `d59dh3vo9af4` | 537（collated_edition） | **不動** |
| `11pj8xx6ouk1s` | `96maqpn6dc` | 94 | **不動** |
| `1agpxlq9l8nb4` | `8rlb6yi1ecqo` | 1 | **不動** |
| `1ahjf6q3b8hds` | `8rlb6yk66qdc` | 1 | **不動** |

**為何 fragments 刪而 collated_edition 不刪**：`chk` 之整理本各項掃的是
`Work/*/*/*/*/collated_edition/*.json`，即 **draft 側那一份**；四庫總目那 537 檔
仍在整理車道手上，刪之則整理本諸驗全部落空。輯佚檔則無此牽連，且此前
莊子／慎子／墨子／商子四條同型殘留刪後全綠，有先例。

**未決**：promote 該不該在拷貝後刪 draft 原件？刪則 collated_edition 的整理流程
要一併改到 production 側去掃；不刪則每升一條帶 fragments 的書就多一條不符。
現行辦法是升格後手動刪 fragments、留 collated_edition。此事牽動整理車道，宜與之議定。

## ~~目錄分片退化~~ → **已施行**（2026-09-03 覆核）

**分片改制已施行完畢**，本節留作沿革。2026-09-03 實測兩倉抽樣 3,000 檔，
分片鍵**全數已取 id 尾三字元**（`Work/<id[-3]>/<id[-2]>/<id[-1]>/`），
前三字元之舊式一檔不存。遷移之 commit 見 `分片改制搬遷 N/8：…純改名`
（book-index-draft）與 `分片改制搬遷 N/6`（book-index）。

**遺留一事——`chk.py` 於 Windows 誤報全庫錯置（判準是對的，錯在路徑分隔）**：
〈目錄分片錯置〉之判準**已隨改制改為尾三字元**（`[0][-3:]`），無誤；
然其比對寫作 `… != p.split('/')[1:4]`，而 `glob` 在 Windows 回的是
`Work\0\0\0\xxx.json`（反斜杠），`split('/')` 切不開遂得空表，於是
**全庫 140,210 檔盡數誤報為錯置**（2026-09-03 實跑所見）。

修法一行——比對前先正規化：

```python
if list(os.path.basename(p).split('-',1)[0][-3:]) != p.replace(os.sep,'/').split('/')[1:4]
```

**在 Linux／WSL 上此驗是綠的**，故此坑唯 Windows 現。本項不改則此驗每跑必爆，
且會把真問題淹掉。

原病灶之描述（存查）：分片鍵原取 id 前三字元，而 snowflake 之高位編的是
status＋type＋時戳高位，故同倉同型同代之 id 前綴必然全同，實測兩倉 150,318 檔
只落進 20 個目錄，最大一格 71,076 檔。方案文件見
`overview/项目进展/古籍索引网站/分片退化-迁移方案.md`。

## 整理本已移入 `book-text` 仓（2026-09-03 覆核所得，前此各記皆未反映）

**整理本／輯佚／全文／抓取素材已整體遷出兩倉，入 `D:\workspace\book-text`**
（commit `036a055dd3`「文本移出：整理本／輯佚／全文／抓取素材遷入 book-text」）。
2026-09-03 實測：

| 倉 | collated_edition |
|---|---|
| `book-index-draft` | **0**（已全刪） |
| `book-index` | 3 部（`補宋書藝文志` 聶崇岐本／王仁俊本、`千頃堂書目`，皆整理車道在手者） |
| `book-text` | **87 部、2,421 檔、97,877 節、102,681 處 work_id 引用** |

**故凡掃整理本者，路徑一律當指 book-text**，掃兩倉只會掃到 3 部而**靜默顯綠**
——此正是 `dd0a23ce315` 所戒之「諸驗掃 0 檔而靜默顯 0，看著全綠而其實空驗」。

**`chk.py` 已跟上，毋須再改**：其 `_assets()` 已取 `_TEXT_ROOT`（`../book-text`）
與 `_PROD_ROOT` 並掃，且兼掃 `collated_edition/*.json` 與歸一後之
`collated_edition/juan/*.json` 兩種深度（2026-09-03 實測掃得 51＋1,221 檔）。
**要當心的是自己另寫的一次性腳本**——凡只掃 draft／production 兩倉者，
掃整理本必空，而空掃是不報錯的。

**又：`_has_collated` 不是壞的，別去「修」。** 兩倉有 47 條 Work 之
`_has_collated=True` 而本倉無其目錄——逐條比對 book-text，**47 條全數在彼，一條不缺**。
派生欄記的是「此書有整理本」之事實，與整理本存於哪個倉無涉。

## `curate_lib.merge_ib` 不寫 `merged_from`——production 內並條必自行補記

`validate-promotions` 之 **E07** 判撞號之準是「存者若**全無** `merged_from` 即斷為撞號」
（`promotion.py:509`，坑30 之精修）。而 `curate_lib.merge_ib` **不寫此欄**。

故凡**production 內並條**（甲之 production 條併入乙之 production 條）而用 merge_ib 者，
併訖必在存者補記，否則 E07 立報撞號——因被併之條原有 draft 墓碑指著它，
併後那個墓碑改指存者，遂成「一 production id 為二 draft 記錄所指」而無並條之痕。

```python
tgt['merged_from'] = '<被併之 production id>'
# 或記於所併入之 indexed_by 節上（_merge_marks 二者皆認）
```

**跨庫並不觸發**（源是 draft 而從未升格，無 promotions 目，不生一對多），
故此坑唯 production 內並條時現。2026-08-25 無斷代輪併《高郵志》二條時踩到。

## 跨庫併條必自行搬 Book——`repoint_all` 不改 `Book.work_id`

`curate_lib.repoint_all` **只掃 `Work/*/*/*/*/collated_edition/*.json`**（見其實作），
改的是整理本節之 `work_id`／`work_ids`。它**不碰 Book 條目之 `work_id`**。

而 `merge_work`（同倉併）另有 `move_books(s, t, books, root)` 一步搬 Book；
**手工作跨庫併（坑21）者若照抄 merge_ib／merge_titles／merge_rel／repoint_all 四步，
就會漏掉這一步**——源條之 Book 仍指著已刪之 work id，成孤兒，
`chk` 之〈Book→Work 單向〉即現（其計數自基線 0 升起即是此類）。

跨庫併之完整步驟，於 merge_ib 諸步之外另須：

```python
for bid in (s.get('books') or []):          # 源條之 Book 改指存留者
    bp = find(bid, root_of_that_book)
    b = rd(bp); b['work_id'] = tgt; wr(bp, b)
    t.setdefault('books', []).append(bid)   # 存留 Work 補反向鏈
    # Book 或 Work 在 production 者，各自 bump_revision
```

2026-08-25 無斷代輪跨庫併 84 條時漏此步，遺 7 個孤兒 Book（涉 4 個被併之 work），已修。

## 跨庫併條：production 側之引用無人改，`merged_from` 亦無人寫

上一條記的是 Book 之漏；此處記其餘二漏——三者同一病根：
`repoint_all` 與 `merge_ib` 都是**為同倉併寫的**，跨庫併照抄即漏。

**一、`repoint_all(src, tgt, root)` 只掃它拿到的那個 `root`。**
跨庫併時 root 傳的是 draft（源在 draft），於是 production 側凡指著源之
`work_id`（整理本節、叢書卷冊映射）與 related_works 之 `id` 一概不改。
源檔一刪，這些引用即懸空——而 `validate-promotions` 看不見（源未升格，
不在 promotions 表內，E04 不管），`chk` 之〈懸空關聯〉亦只掃 draft。
**兩庫都要掃**：

```python
repoint_all(src, tgt, ROOT)    # draft
repoint_all(src, tgt, PROD)    # production —— 這一行常被漏掉
```

**二、`merge_ib` 不寫 `merged_from`。**（前文已記其於 production 內並條之害；
跨庫併同樣中招。）不寫則來歷失載：主條看不出它曾併過誰，
日後見同題二 id 亦不知其為併。跨庫併之後須自行補：

```python
t.setdefault('merged_from', []).append(src)
```

2026-08-25 無斷代輪跨庫併 84 條，二漏俱全：production 側 114 處引用懸空、
84 條主條無 `merged_from`，事後補掃始得。
**故：跨庫併不可照抄同倉之四步，須用一份專為跨庫寫的函式。**

## `promotions.json` 非原子寫——程序被殺即留下半個壞檔

`PromotionsStore.save` 之寫盤是 `open(self.path, "w")` 就地截斷再 `json.dump`
（`book_index_manager/promotion.py`）。而 promote 是**每升一條存一次**，
於是這個「先截斷、後寫滿」的窗口每條書都開一次。程序若在窗口裡死掉
——被 `timeout` 的 SIGTERM 攔腰砍、被 Ctrl-C、機器斷電——磁上留下的
就是一個截了一半的 `promotions.json`。

2026-08-25 末批升格實遇：`timeout 900` 到點發 SIGTERM，77,392 目之檔
只剩 81,103 行（應為 386,965 行），`json.load` 當場報
`Expecting ':' delimiter`。**升格記錄是唯一一份 draft↔production 的對照表，
它壞了，`validate-promotions`、`sweep-promoted-refs`、撞號檢測全部失效。**

**救法**（本次即如此救回，一目未失）：

1. `git show HEAD:promotions.json` 取回上一次提交之完整檔——**故升格前必先提交**；
2. 掃 draft 之 `_promoted_to`／`_promoted_at` 與 production 檔之在否，
   判出「已升而記錄失載」者，逐條補回；
3. 補完必驗一對多之數不增（增即是撞號覆寫，見〈promote 撞號〉一則）。

**防法**：寫暫存檔再 `os.replace`（同檔案系統上原子），殺在任何一刻，
磁上要麼是舊的完整檔、要麼是新的完整檔：

```python
tmp = str(self.path) + '.tmp'
with open(tmp, 'w', encoding='utf-8') as f:
    json.dump(payload, f, indent=2, ensure_ascii=False)
    f.write('\n')          # SCHEMA〈JSON 書寫格式〉：檔尾一個換行
    f.flush(); os.fsync(f.fileno())
os.replace(tmp, self.path)
```

眼下以 monkeypatch 行之（`promote_rest.py`），**當入 book-index-manager**。

**又：長批升格不要套 `timeout`。** 逐條引用重寫要掃全倉，而 15 萬檔只落在
20 個目錄（見〈分片退化〉），每條都掃一遍即分鐘級。長批當用
`--no-rewrite-refs` 而收工跑一次 `sweep-promoted-refs.py`——它是冪等的，
掃一次與逐條掃等價，且不必賭在超時之前跑完。

## 墓碑 stub 化：封賬之前必先平賬，且三處工具要跟著改

「stub 化」是把已升格之 draft 墓碑縮為升格印記——
`{schema_version,id,type,title,_promoted_to,_promoted_at}` ＋ `merged_from`
（溯源）＋ `_has_*`（派生）＋ 一句指向 production 之 `ai_note`。
其用不在省空間（77,373 條 66.2 MB → 25 MB，而 `.git` 不縮反漲），
**在防誤改**：墓碑留著全欄位，後續腳本與人就會接著改它，而 production
才是 canonical。2026-08-26 全量行之，記其要：

**一、stub 是封賬，封之前賬要平。** 墓碑一縮，其獨有之知識即永失。
本輪封賬前掃出並補畢者：production 側 52 處懸空 entity_id（2026-08-24
entity 品質清整退役一批非人 entity，draft 側改繫／撤繫而 production 未跟）、
2 條 description（撰人生平之考只在墓碑）、2 條懸空 related_works。
**比對之際有兩個假陽性坑**：
  - `merged_from` 是溯源欄，兩側本就皆是 draft id，歸一化時**不可動**
    ——動之則每條帶併記之 `indexed_by` 都報（實測 120 條假陽）；
  - production `revision` > 1.0.0 者是升格後正當改過，不算不一致。
去此二者，54,021 條裡真須人判的只有 30 條。

**二、`stub-tombstones.py` 原有二 bug**（已修）：`STUB_KEEP_KEYS` 用的是
**無前綴**之 `promoted_to`/`promoted_at`，而現行墓碑是 `_promoted_to`
——照跑會把升格印記整個刪掉，即 E02/E03 所報之最嚴重破壞；寫檔又漏了
檔尾換行。今並加 `--ids-from` 以名單驅動（其自帶之 diff 無上述二保護，
大批次宜由呼叫方驗過再喂）。

**三、stub 之後三處驗要跟著改**：
  - `validate-promotions` 之 **E08** 逐欄比墓碑與正身，而 stub 之 `ai_note`
    與正身必異——全量 stub 後實測報 **70,360 條**，把此驗徹底淹掉。
    今於 promotion.py 加判：墓碑之欄位集合若不出 stub 之範圍即整條跳過
    （judge by 欄位集合，不 judge by ai_note 之措辭，免得改文案即失效）。
  - `chk` 之〈作品之 entity_id 指向已退役者〉原作 `for _w,_e in IW.items()`
    ——**只掃 draft 之 works 索引**。斷代諸輪畢後正身盡在 production、
    draft 只剩墓碑，此驗遂名存實亡：實測 production 有 52 處懸空而它報 0。
    今兩庫之 Work 與 Book 一併掃。
  - `chk` 之〈派生欄位與重算不符〉：`_has_collated` 等派生欄記的是
    **draft 側**有無資產目錄，而志書之整理本依遷移方案仍留 draft，
    故升格後仍為真。stub 不可刪之（實測刪後報 39 條）。

**四、「已 stub」之判據取欄位集合，勿取 ai_note 之文字。** 本輪先以正則
`依例 stub 化` 判，漏了 18 條——那是前一輪（G8）**寫了 stub 說明句而欄位
沒刪乾淨**者，文字判據遂誤認其已 stub 而跳過。
